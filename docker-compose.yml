version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: luxury-real-estate-app
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
